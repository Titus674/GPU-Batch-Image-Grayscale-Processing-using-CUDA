# GPU-Batch-Image-Grayscale-Processing-using-CUDA
This project demonstrates GPU-based image processing using CUDA.  A CUDA kernel processes image data in parallel to perform grayscale conversion on multiple images.  The program loads a dataset of images, transfers them to GPU memory, executes the kernel, and saves the processed results.
